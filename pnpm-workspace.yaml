allowBuilds:
  esbuild: true
  ffmpeg-static: true
  audify: true
minimumReleaseAge: 10080 # 7 days, in minutes
minimumReleaseAgeExclude:
  # Our own package; every published version is newer than the 7-day cutoff
  - kitty-motion
